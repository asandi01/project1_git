@model IEnumerable<Project1.Models.PaymentRecordModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/shared/_LayoutPage.cshtml";
}
<!-- Page Heading -->
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">
            Blank Page
            <small>Subheading</small>
        </h1>
        <ol class="breadcrumb">
            <li>
                <i class="fa fa-dashboard"></i>  <a href="index.html">Dashboard</a>
            </li>
            <li class="active">
                <i class="fa fa-file"></i> Blank Page
            </li>
        </ol>
    </div>
</div>
<!-- /.row -->

<div class="row">
    <div class="col-xl-12">
        <h2 class="page-header">Flot Charts</h2>

        <p>
            @Html.ActionLink("Create New", "Create")
        </p>
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.idUser)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.detail)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.amount)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.recurrence)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.recurrenciaTypeId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.paymentDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.providerId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.expenseCategoryId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Now)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.idUser)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.detail)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.amount)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.recurrence)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.recurrenciaTypeId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.paymentDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.providerId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.expenseCategoryId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Now)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.id }) |
                        @Html.ActionLink("Details", "Details", new { id = item.id }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.id })
                    </td>
                </tr>
            }

        </table>

    </div>
</div>
